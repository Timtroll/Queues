% layout 'default';
% title 'Welcome 123';
<h2><%= $msg %></h2>

	<form action="/addjob" method="post">
		<input type="text" placeholder="google.com" name="url">
		<button type="submit">Add</button>
	</form>

	<hr>
% if (keys %$jobs) {
	<li>Running process</li>
%	foreach (sort {$a <=> $b} keys %$jobs) {
	pid = <a href="/status?pid=<%= $_ %>" target="_blank"><%= $_ %></a> (<%= $jobs->{$_}->{command} %>) <a href="/killer?pid=<%= $_ %>">kill</a><br>
%	}
% } else {
	<li>Nothing</li>
% }
